[project]
name = "szl-ouroboros"
version = "0.1.0"
description = "Bounded loop-tax accounting kernel. Software, not a trained model."
readme = "README.md"
license = { text = "Apache-2.0" }
requires-python = ">=3.9"

[tool.pytest.ini_options]
testpaths = ["tests"]
